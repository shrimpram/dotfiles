#!/usr/bin/env sh

set -e

export TEMP
TEMP="$(mktemp)"

grep -E '^Subject: |^From: ' | sed 's/.*: //; s/ <.*@.*>//; 2s/\(.*\)/(\1)/; 1s/^/Email from /' | paste -d " " - - > "$TEMP"

todoist add "$(cat "$TEMP")" --label-ids "2163180572"

rm "$TEMP"
