#!/usr/bin/env sh

set -e

export TEMP
TEMP="$(mktemp)"

grep -E 'Subject: |From: ' | sed 's/.*: //; s/ <.*@.*>//; 2s/\(.*\)/(\1)/; 1s/^/Email from /' | paste -d " " - - > "$TEMP"

task add +email "$(cat "$TEMP")"

rm "$TEMP"
